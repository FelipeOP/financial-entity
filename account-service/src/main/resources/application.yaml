spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/accounts
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port: 8082

logging:
  level:
    root: INFO
