spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/transactions
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

server:
  port: 8081

logging:
  level:
    root: INFO
